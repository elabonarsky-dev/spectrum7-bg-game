Spectrum 7 uses the Web Audio API (see js/audio.js) for spin and stop sounds — no MP3/WAV files are required.

You can add your own loop/clunk samples later and swap them in from this folder if you prefer sampled audio.
